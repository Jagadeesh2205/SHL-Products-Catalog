# Quick Start Guide - SHL Assessment Recommendation System

## 🚀 Get Started in 5 Minutes

### Step 1: Install Dependencies
```bash
# Windows
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Step 2: Get Free Google Gemini API Key
1. Visit: https://ai.google.dev/
2. Click "Get API Key" 
3. Copy your key

### Step 3: Configure Environment
```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

Edit `.env` and add:
```
GOOGLE_API_KEY=your_key_here
```

### Step 4: Run Complete Setup
```bash
python run.py
```

This will:
- ✅ Scrape SHL catalog (377+ assessments)
- ✅ Generate embeddings
- ✅ Test the system
- ✅ Generate predictions
- ✅ Start the API server

### Step 5: Access the Application
Open your browser: **http://localhost:5000**

---

## 📝 Alternative: Step-by-Step Setup

### 1. Scrape SHL Catalog
```bash
python src/scraper.py
```
Creates: `data/scraped_data.json`

### 2. Generate Embeddings
```bash
python src/embeddings.py
```
Creates: `data/embeddings/` folder

### 3. Test Recommender
```bash
python src/recommender.py
```

### 4. Start API
```bash
python api/app.py
```

### 5. Generate Predictions
```bash
python src/generate_predictions.py
```
Creates: `predictions.csv`

---

## 🧪 Quick Test

### Test API with curl
```bash
# Health check
curl http://localhost:5000/health

# Get recommendations
curl -X POST http://localhost:5000/recommend \
  -H "Content-Type: application/json" \
  -d "{\"query\": \"Java developer\"}"
```

### Test with Python
```python
import requests

response = requests.post(
    'http://localhost:5000/recommend',
    json={'query': 'Java developer with communication skills'}
)
print(response.json())
```

---

## 📦 What Gets Created

```
SHL/
├── data/
│   ├── scraped_data.json      ← 377+ assessments
│   └── embeddings/            ← Vector database
│       ├── embeddings.npy
│       ├── faiss.index
│       └── metadata.pkl
├── predictions.csv            ← Test predictions
└── .env                       ← Your API key
```

---

## 🎯 Example Queries to Try

1. **Technical + Soft Skills:**
   ```
   Java developer who can collaborate with business teams
   ```

2. **Multiple Technologies:**
   ```
   Python, SQL, and JavaScript proficiency
   ```

3. **Role-Based:**
   ```
   Leadership and management assessment
   ```

4. **Specific Types:**
   ```
   Cognitive and personality tests for analyst role
   ```

---

## 🔧 Common Commands

### Restart API
```bash
python api/app.py
```

### Regenerate Data
```bash
python setup.py
```

### Run Tests
```bash
python tests/test_api.py
```

### Generate New Predictions
```bash
python src/generate_predictions.py
```

---

## 🚀 Deploy to Cloud (Free)

### Render.com
```bash
# 1. Push to GitHub
git init
git add .
git commit -m "Initial commit"
git push origin main

# 2. Go to render.com
# 3. New Web Service → Connect repo
# 4. Add GOOGLE_API_KEY env var
# 5. Deploy!
```

---

## ⚡ Troubleshooting

### Issue: Port 5000 already in use
```bash
# Windows - Find and kill process
netstat -ano | findstr :5000
taskkill /PID <process_id> /F

# Linux/Mac
lsof -i :5000
kill -9 <process_id>

# Or use different port
set PORT=8000
python api/app.py
```

### Issue: Module not found
```bash
pip install -r requirements.txt
```

### Issue: No embeddings
```bash
python setup.py
```

### Issue: API key error
Check `.env` file has:
```
GOOGLE_API_KEY=your_actual_key_here
```

---

## 📊 File Submissions

For SHL submission, you need:

1. **predictions.csv** ✅ (generated by `src/generate_predictions.py`)
2. **GitHub URL** ✅ (create repo and push)
3. **API URL** ✅ (deploy to Render/Railway)
4. **Web App URL** ✅ (same as API URL)
5. **Approach Document** ✅ (APPROACH.md)

---

## 💡 Pro Tips

1. **Fast Setup:** Use `python run.py` for one-command setup
2. **Testing:** Try example queries in web UI before API testing
3. **Performance:** First API call may be slow (model loading)
4. **Deployment:** Render free tier is easiest for beginners
5. **Dataset:** Ensure `Gen_AI Dataset.xlsx` is in root folder

---

## 📞 Need Help?

- **Documentation:** Check README.md
- **Deployment:** See DEPLOYMENT.md
- **Technical Details:** Read APPROACH.md
- **Test API:** Run `python tests/test_api.py`

---

## ✅ Success Checklist

Before submission, verify:

- [ ] `python run.py` completes without errors
- [ ] Web UI loads at http://localhost:5000
- [ ] API returns recommendations for test query
- [ ] `predictions.csv` exists with test results
- [ ] Code pushed to GitHub (public or private)
- [ ] App deployed and URL accessible
- [ ] APPROACH.md reviewed
- [ ] All submission materials ready

---

**⚡ Ready to start? Run:**
```bash
python run.py
```

**🎉 That's it! Your SHL Recommendation System is ready!**
